# 🚀 راهنمای سریع Tunnel برای Backend

## مشکل: Products نمایش داده نمی‌شوند

وقتی از ngrok استفاده می‌کنید، باید **Backend را هم tunnel کنید**.

## ✅ راه حل سریع (4 مرحله):

### 1️⃣ Terminal جدید - اجرای Backend:
```bash
npm run server
```

### 2️⃣ Terminal جدید دیگر - Tunnel برای Backend:
```bash
ngrok http 3001
```

**کپی کنید URL که نمایش داده می‌شود** (مثلاً: `https://xyz789.ngrok-free.app`)

### 3️⃣ ایجاد یا ویرایش `.env.local`:

در root پروژه فایل `.env.local` ایجاد کنید:
```env
VITE_API_URL=https://xyz789.ngrok-free.app
```

**⚠️ مهم:** URL backend را که از ngrok گرفتید اینجا بگذارید!

### 4️⃣ Restart Frontend:

**Terminal که `npm run dev` در حال اجرا است را متوقف کنید** (Ctrl+C) و دوباره اجرا کنید:
```bash
npm run dev
```

## ✅ حالا باید کار کند!

- Frontend: `https://49176a1729ee.ngrok-free.app` (از ngrok frontend)
- Backend: `https://xyz789.ngrok-free.app` (از ngrok backend - این را در `.env.local` بگذارید)

## 🔍 اگر هنوز کار نمی‌کند:

1. مطمئن شوید Backend در حال اجرا است (`npm run server`)
2. مطمئن شوید Backend tunnel شده است (`ngrok http 3001`)
3. URL backend را در `.env.local` درست کپی کرده‌اید
4. Frontend را restart کرده‌اید

## 💡 نکته:

هر بار که ngrok را restart می‌کنید، URL تغییر می‌کند. باید `.env.local` را به‌روزرسانی کنید و Frontend را restart کنید.

